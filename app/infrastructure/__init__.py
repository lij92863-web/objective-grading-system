"""Infrastructure layer — I/O, storage, and external adapters.

Contains concrete implementations of file readers, Excel/CSV/HTML
exporters, and storage helpers.  These depend on domain interfaces,
not the other way around.
"""
