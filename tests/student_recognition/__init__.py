"""Test package for the Student Recognition Engine (SRE945 / SRE121) suite.

Marker so ``unittest discover`` can recurse into this directory from the project
root (e.g. ``python run_tests.py`` / ``discover("tests", "test*.py")``).
"""
