"""Report data builders — pure analysis logic, no I/O.

These functions transform grading results into dict rows ready for
CSV/Excel/HTML exporters.  They do NOT import legacy, write files,
or access the network.
"""
